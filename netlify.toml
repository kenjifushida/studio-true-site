[build]
  # Skip building on Netlify - deploy pre-built site instead
  # Use: npm run build && netlify deploy --prod
  command = "echo 'Skipping build - deploy pre-built site'"
  publish = "public"

[build.environment]
  NODE_VERSION = "18"
